import React, { useState, useEffect } from 'react';
// import {InputGroup, FormControl, Card, CardDeck} from 'react-bootstrap';
import {Space, Input, Card, Avatar, Rate} from "antd";
import axios from "axios"

let Albums = () => {
	let [status, setStatus] = useState(false);

	useEffect(() => {
		console.log("Songs");
	});

	return
};

export default Albums;